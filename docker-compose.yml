services:
  cascade-proxy:
    build:
      context: ./cpp_core
      dockerfile: Dockerfile
    image: cascade-router-proxy:latest
    container_name: cascade-proxy
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models:ro
      - ./logs:/app/logs
    restart: unless-stopped
